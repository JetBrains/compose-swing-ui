package org.jetbrains.compose.swing.test.lookalike.second

import javax.swing.JPanel

/**
 * A widget of a caller's own, sharing its simple name with
 * [org.jetbrains.compose.swing.test.lookalike.first.Widget] and nothing else.
 */
class Widget : JPanel()
